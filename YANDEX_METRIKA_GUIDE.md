# Руководство по настройке Яндекс.Метрики

## 📊 Что отслеживается

### 1. ✅ Отправка формы обратной связи (модалка)
**📍 Файл:** `src/components/modal/ContactModal.tsx` (строки 114-120)  
**Поля формы:** Имя, Телефон, Почта (необязательно), Бюджет, Описание задачи  
**Цель:** `form_submit` с параметром `form_name: contact_form`

### 2. ✅ Подписка на рассылку (через бургер меню)
**📍 Файл:** `src/components/header/HeaderNewsLetter.tsx` (строки 39-44)  
**Поле формы:** Email  
**Цель:** `form_submit` с параметром `form_name: newsletter_subscribe`

### 3. ✅ Клик на Telegram
**📍 Файлы:** 
- `src/components/social/FooterSocial.tsx` (строки 42-55)
- `src/components/social/SocialShareV3.tsx` (строки 11-24)  
**Цель:** `click` с параметром `element_name: telegram_click`

### 4. ✅ Клик на WhatsApp
**📍 Файлы:** 
- `src/components/social/FooterSocial.tsx` (строки 57-71)
- `src/components/social/SocialShareV3.tsx` (строки 26-40)  
**Цель:** `click` с параметром `element_name: whatsapp_click`

### 5. ✅ Клик на телефон
**📍 Файлы:** 
- `src/components/social/FooterSocial.tsx` (строки 13-18) - кнопка копирования
- `src/components/project/ProjectIdeaV1.tsx` (строки 16-28) - ссылка tel:
- `src/components/contact/ContactV1.tsx` (строки 19-30) - ссылка tel:
- `src/components/footer/FooterV1.tsx` (строки 57-66) - ссылка tel:
- `src/components/footer/FooterV2.tsx` (строки 43-52) - ссылка tel:
- `src/components/footer/FooterV4.tsx` (строки 43-52) - ссылка tel:
**Цель:** `click` с параметром `element_name: phone_click`

## 🎯 Настройка целей в интерфейсе Яндекс.Метрики

### Цель 1: Отправка формы обратной связи
1. Перейдите: **Настройки → Цели → Добавить цель**
2. **Название:** Отправка формы обратной связи
3. **Тип:** JavaScript-событие
4. **Идентификатор цели:** `form_submit`
5. **Условие:** `form_name == contact_form`

### Цель 2: Подписка на рассылку
1. **Название:** Подписка на рассылку
2. **Тип:** JavaScript-событие
3. **Идентификатор цели:** `form_submit`
4. **Условие:** `form_name == newsletter_subscribe`

### Цель 3: Клик на Telegram
1. **Название:** Клик на Telegram
2. **Тип:** JavaScript-событие
3. **Идентификатор цели:** `click`
4. **Условие:** `element_name == telegram_click`

### Цель 4: Клик на WhatsApp
1. **Название:** Клик на WhatsApp
2. **Тип:** JavaScript-событие
3. **Идентификатор цели:** `click`
4. **Условие:** `element_name == whatsapp_click`

### Цель 5: Клик на телефон
1. **Название:** Клик на телефон
2. **Тип:** JavaScript-событие
3. **Идентификатор цели:** `click`
4. **Условие:** `element_name == phone_click`

## 🤖 Промпт для ChatGPT (помощник в Яндекс.Метрике)

Скопируйте этот промпт и вставьте в ChatGPT, когда работаете с Яндекс.Метрикой:

```
Я работаю с Яндекс.Метрикой для сайта CDI Agency (ID счетчика: 105676447). 

Мне нужно настроить отслеживание следующих событий:

1. Отправка формы обратной связи (модалка с полями: имя, телефон, email, бюджет, описание)
   - Цель: form_submit
   - Параметр: form_name = contact_form

2. Подписка на рассылку (форма в бургер меню, поле: email)
   - Цель: form_submit  
   - Параметр: form_name = newsletter_subscribe

3. Клик на Telegram
   - Цель: click
   - Параметр: element_name = telegram_click

4. Клик на WhatsApp
   - Цель: click
   - Параметр: element_name = whatsapp_click

5. Клик на телефон (копирование номера или клик по ссылке tel:)
   - Цель: click
   - Параметр: element_name = phone_click

Помоги мне:
- Правильно настроить эти 5 целей в интерфейсе Яндекс.Метрики
- Создать отчеты для анализа этих событий отдельно (каждое событие должно быть видно отдельно)
- Настроить сегменты для фильтрации данных
- Объяснить, как интерпретировать результаты по каждому событию
- Предложить оптимизации на основе данных

Все цели уже настроены в коде сайта и отправляют события через JavaScript. Мне нужна помощь с настройкой в интерфейсе Метрики и анализом данных.

Важно: каждое событие должно отслеживаться отдельно, чтобы я мог видеть:
- Сколько заявок через форму обратной связи
- Сколько подписок на рассылку
- Сколько кликов на Telegram
- Сколько кликов на WhatsApp  
- Сколько кликов на телефон

Все значения должны быть отдельными и не смешиваться.
```

## 📝 Где находится код отслеживания

### 1. Форма обратной связи (модалка)
**Файл:** `src/components/modal/ContactModal.tsx` (строки 114-120)

```typescript
// Отслеживание отправки формы
const { trackFormSubmit } = await import('../../utils/yandex-metrika');
trackFormSubmit('contact_form', {
  form_type: 'contact',
  has_budget: !!formData.budget,
  has_description: !!formData.description,
});
```

### 2. Форма подписки на рассылку (бургер меню)
**Файл:** `src/components/header/HeaderNewsLetter.tsx` (строки 39-44)

```typescript
// Отслеживание подписки на рассылку
const { trackFormSubmit } = await import('../../utils/yandex-metrika');
trackFormSubmit('newsletter_subscribe', {
  form_type: 'newsletter',
  email_provided: true,
});
```

### 3. Клик на Telegram
**Файлы:** 
- `src/components/social/FooterSocial.tsx` (строки 46-51)
- `src/components/social/SocialShareV3.tsx` (строки 15-20)

```typescript
onClick={() => {
  trackClick('telegram_click', 'link', {
    action: 'open_telegram',
    url: TELEGRAM_URL,
  });
}}
```

### 4. Клик на WhatsApp
**Файлы:** 
- `src/components/social/FooterSocial.tsx` (строки 62-67)
- `src/components/social/SocialShareV3.tsx` (строки 31-36)

```typescript
onClick={() => {
  trackClick('whatsapp_click', 'link', {
    action: 'open_whatsapp',
    url: WHATSAPP_URL,
  });
}}
```

### 5. Клик на телефон
**Файлы:**
- `src/components/social/FooterSocial.tsx` (строки 14-18) - кнопка копирования
- `src/components/project/ProjectIdeaV1.tsx` (строки 16-28) - ссылка tel:
- `src/components/contact/ContactV1.tsx` (строки 19-30) - ссылка tel:
- `src/components/footer/FooterV1.tsx` (строки 57-66) - ссылка tel:
- `src/components/footer/FooterV2.tsx` (строки 43-52) - ссылка tel:
- `src/components/footer/FooterV4.tsx` (строки 43-52) - ссылка tel:

```typescript
onClick={() => {
  trackClick('phone_click', 'button', {
    action: 'copy_phone' или 'call_phone',
    phone: '+79853656294',
  });
}}
```

## 🔍 Проверка работы

### В консоли браузера:

```javascript
// Проверить загрузку Метрики
window.ym

// Проверить dataLayer
window.dataLayer

// Вручную отправить событие для тестирования
window.ym(105676447, 'reachGoal', 'form_submit', { form_name: 'contact_form' });
window.ym(105676447, 'reachGoal', 'click', { element_name: 'telegram_click' });
```

### В интерфейсе Метрики:

1. **Отчеты → Онлайн** - проверьте активных посетителей
2. **Отчеты → Стандартные отчеты → Цели** - проверьте работу всех 5 целей
3. **Отчеты → Вебвизор** - посмотрите записи сессий

## 📊 Создание отчетов

### Отчет 1: Конверсии по формам
1. **Отчеты → Конструктор отчетов → Создать отчет**
2. **Метрики:** Количество достижений цели
3. **Группировка:** По целям
4. **Фильтр:** Цели содержат "form_submit"
5. **Разделить по:** form_name (contact_form и newsletter_subscribe будут отдельно)

### Отчет 2: Клики по контактам
1. **Отчеты → Конструктор отчетов → Создать отчет**
2. **Метрики:** Количество достижений цели
3. **Группировка:** По целям
4. **Фильтр:** Цели содержат "click"
5. **Разделить по:** element_name (telegram_click, whatsapp_click, phone_click будут отдельно)

### Отчет 3: Сводка всех событий
1. **Отчеты → Конструктор отчетов → Сводная таблица**
2. Добавьте все 5 целей как отдельные метрики
3. Группировка: По датам
4. Это покажет все события в одной таблице

## 📈 Рекомендации по анализу

1. **Ежедневно:** Проверяйте количество отправленных форм и кликов по контактам
2. **Еженедельно:** Анализируйте, какой канал связи (Telegram/WhatsApp/Телефон) наиболее популярен
3. **Ежемесячно:** Сравнивайте эффективность разных форм (обратная связь vs подписка)

## ⚠️ Важно

- После настройки целей подождите 24 часа для сбора данных
- Все цели уже настроены в коде и работают автоматически
- Каждое событие отслеживается отдельно с уникальным идентификатором
- Используйте промпт для ChatGPT, если нужна помощь с настройкой в интерфейсе

---

**ID счетчика:** 105676447  
**Сайт:** https://cdiagency.ru

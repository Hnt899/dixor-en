# Telegram Bot для управления заказами

Telegram-бот на Python (aiogram 3) для приёма заявок с сайта и управления заказами через Telegram.

## Функциональность

- 🕒 **Заказы ожидающие принятия** - просмотр и принятие новых заказов
- 🔧 **Заказы в работе** - управление активными заказами (оранжевый/зелёный статус)
- 📚 **История заказов** - просмотр выполненных и отменённых заказов
- 📡 **Вебхук для приёма заявок** - автоматическое добавление заявок с сайта

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

3. Узнайте свой Telegram ID (можно через [@userinfobot](https://t.me/userinfobot))

4. Настройте конфигурацию в `config.py`:
   - Установите `BOT_TOKEN` (или через переменную окружения)
   - Добавьте ID администраторов в `ADMIN_IDS`
   - Настройте параметры вебхука (если нужно)

## Запуск

### Локальный запуск (для разработки)

Для локального тестирования можно использовать polling вместо вебхука. Создайте файл `bot_polling.py`:

```python
import asyncio
import logging
from aiogram import Bot, Dispatcher
from aiogram.fsm.storage.memory import MemoryStorage
from config import BOT_TOKEN
from handlers import admin

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

async def main():
    bot = Bot(token=BOT_TOKEN)
    dp = Dispatcher(storage=MemoryStorage())
    dp.include_router(admin.router)
    
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
```

Запуск:
```bash
python bot_polling.py
```

### Запуск с вебхуком (для продакшена)

```bash
python bot.py
```

## Интеграция с сайтом

Для отправки заявок с сайта на бота, отправьте POST-запрос на endpoint `/new_order`:

```javascript
// Пример на JavaScript
async function sendOrderToBot(orderData) {
    const response = await fetch('https://your-domain.com/new_order', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            name: orderData.name,
            phone: orderData.phone,
            email: orderData.email, // опционально
            comment: orderData.comment // опционально
        })
    });
    
    const result = await response.json();
    return result;
}
```

### Формат запроса

```json
{
    "name": "Иван Иванов",
    "phone": "+79991234567",
    "email": "ivan@example.com",  // опционально
    "comment": "Нужна консультация"  // опционально
}
```

### Формат ответа

Успешный ответ:
```json
{
    "success": true,
    "order_number": "12345"
}
```

Ошибка:
```json
{
    "error": "Missing required fields: name, phone"
}
```

## Структура проекта

```
telegram_bot/
├── bot.py              # Главный файл запуска бота
├── config.py           # Конфигурация (токен, админы, вебхук)
├── database.py         # Работа с SQLite базой данных
├── states.py           # FSM состояния
├── handlers/           # Обработчики
│   ├── __init__.py
│   ├── admin.py        # Хэндлеры для админа
│   └── webhook.py      # Обработка вебхуков с сайта
├── requirements.txt    # Зависимости Python
└── README.md           # Документация
```

## База данных

Бот использует SQLite для хранения заказов. База данных создаётся автоматически при первом запуске в файле `telegram_bot/orders.db`.

### Структура таблицы orders:

- `id` - уникальный идентификатор
- `order_number` - 5-значный номер заказа
- `name` - имя клиента
- `email` - почта (может быть пустой)
- `phone` - телефон
- `comment` - комментарий (может быть пустой)
- `status` - статус заказа (pending, in_work_orange, in_work_green, completed, cancelled)
- `cancellation_reason` - причина отмены (если заказ отменён)
- `created_at` - дата создания
- `updated_at` - дата обновления

## Статусы заказов

- `pending` - ожидает принятия
- `in_work_orange` 🟠 - в работе (оранжевый индикатор)
- `in_work_green` 🟢 - в работе (зелёный индикатор)
- `completed` 🟢 - выполнен
- `cancelled` 🔴 - отменён

## Команды бота

- `/start` - запустить бота и показать главное меню

## Переменные окружения

Можно использовать переменные окружения вместо редактирования `config.py`:

- `BOT_TOKEN` - токен бота от BotFather
- `WEBHOOK_HOST` - хост для вебхука (по умолчанию: localhost)
- `WEBHOOK_PORT` - порт для вебхука (по умолчанию: 8443)
- `WEBHOOK_PORT_LISTEN` - порт для прослушивания (по умолчанию: 8443)

## Примечания

- Для работы вебхука нужен HTTPS (можно использовать ngrok для локальной разработки)
- Убедитесь, что порт вебхука открыт в файрволе
- Регулярно делайте резервные копии базы данных

